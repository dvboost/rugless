# Rugless
```shell
npx hardhat help
npx hardhat test
npx hardhat vars set PRIVATE_KEY
npx hardhat vars set ETHERSCAN_API_KEY
npx hardhat ignition deploy ignition/modules/RuglessLiquidityPool.ts --network sepolia --verify
```
